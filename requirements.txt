# OfficeHelper 依赖文件
# Python 3.10+

# Word / Office 集成 (Windows COM)
pywin32>=306

# 大模型支持
openai>=1.0.0
anthropic>=0.3.0
requests>=2.31.0

# Web 服务
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# 配置
python-dotenv>=1.0.0
PyYAML>=6.0.1

# 可选
# ollama>=0.0.0        # 本地大模型 (如使用 Ollama)
